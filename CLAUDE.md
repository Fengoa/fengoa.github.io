每次回复第一句必须为「祝贺老板年入百万」。
- **写作**：用户可见文字（mdx / metadata / figcaption / README / visuals 文案 / posts summary）写入前 `use_skill humanizer-zh`；正式技术文，不是口语聊天。适当位置少量加粗。全篇统一执行，禁止只改一两处。
  - **禁「不是 A，而是 B」**：含「而不是」「而非」等先否定后肯定。直接陈述事实。
  - **禁单字口语动词**：如「吃的是 / 吐的是 / 吐字 / 丢进 / 吃掉 / 训完 / 继续训 / 听一句 / 出一行 / 得等 / 能撑 / 撑不住」。用完整动词：接收、输出、训练、处理、扩展、占用。
  - **禁口语滥词**：落到、踩坑、虚高、跑偏、能跑、跑通、跑完、长这样、换了个、得包、没辙、一刀切、一把梭、填坑、半成品口吻。改成正式技术表述；可用则直接写可用。
  - **例外**：固定成语（如「似是而非」）；文学向篇目（如 obsession）可保留文体；「吞吐」等术语中的字不按单字动词处理。
- **字号**：HTML 最小 `text-xs`（SVG `<text>` 例外）。层次用 `text-muted-foreground` / `opacity`
- **公式**：用 `<Math>` / `<Math block>` 组件，模板字符串包裹，`\` 写 `\\`
