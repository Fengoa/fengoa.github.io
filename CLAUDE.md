# 项目协作规则

- **写作**：用户可见文字（mdx / metadata / figcaption / README）写入前 `use_skill humanizer-zh`，同时避免口语化的表达
- **字号**：HTML 最小 `text-xs`（SVG `<text>` 例外）。层次用 `text-muted-foreground` / `opacity`
- **公式**：用 `<Math>` / `<Math block>` 组件，模板字符串包裹，`\` 写 `\\`
