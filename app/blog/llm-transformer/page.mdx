import {
  AblationBarChart,
  TransformerBlockFlow,
  LayerNormEffect,
  FFNExpansion,
  ParamBreakdownPie,
} from "./visuals";

export const metadata = {
  title: "搭一个完整的 Transformer",
}

# 搭一个完整的 Transformer

<SeriesNav series="llm" />

上一篇讲完了 self-attention，但仅靠 attention 无法训练出可用模型。将 attention 单独取出训练，loss 会持续处于高位。GPT-2 中真正生效的是完整 Transformer block：LayerNorm 稳定数值，attention 决定关注对象，残差为信息保留直通通道，FFN 对 attention 输出做进一步非线性变换。

判断一个组件是否必要，最直接的方法是消融实验。在相同的 2 层、128 维、4 头、64 上下文配置下，分别去掉一个组件并训练 3000 步：

<AblationBarChart />

四个组件去掉一个，loss 都会上升 0.2 以上，去掉 LayerNorm 最严重，直接退化到 2.25。后面分别讲清楚每个组件解决的是什么问题。

## 一个 block 内部的数据流

先把整体走一遍，再去看具体的零件。一个 Transformer block 内部，输入 `x` 会过两遍“先 norm 再做事再加回原值”的循环：第一遍是 attention，第二遍是 FFN。

<TransformerBlockFlow />

写成代码就是这样：

```python
def block_forward(x):
    x = x + attn(layer_norm_1(x))   # 第一遍：look around
    x = x + ffn(layer_norm_2(x))    # 第二遍：digest
    return x
```

这种 “先 norm 再算” 叫 Pre-Norm，是 GPT-2 之后的事实标准。原始论文的 Post-Norm 把 norm 放在残差相加之后（`layer_norm(x + attn(x))`），训练更不稳定，深一点就经常崩。

## LayerNorm：把数值幅度压回正常区间

LayerNorm 是这次消融实验里去掉影响最大的一个。它的计算极其简单——把每个位置的向量减去自身均值，再除以标准差：

<Math block>{`\\text{LN}(x) = \\frac{x - \\mu}{\\sqrt{\\sigma^2 + \\epsilon}} \\odot \\gamma + \\beta`}</Math>

`γ` 和 `β` 是两个可学习参数，给模型留一点重新调整尺度的余地。但归一化才是核心操作。

为什么要这么做？看一下不归一化时分布是怎么走的：

<LayerNormEffect />

将上一层 scale 增至 5，分布扩展至 ±15 区间。若不经处理即输入 attention，QK 点积可达数十至数百，softmax 进入饱和区，梯度无法回传。LayerNorm 后向量归一化为均值 0、方差 1，下游对上一层 scale 扰动不敏感。

实现就一行：

```python
def layer_norm(x):
    return (x - x.mean(-1, keepdim=True)) / (x.std(-1, keepdim=True) + 1e-5)
```

工业界的常见替代是 RMSNorm，省掉减均值那一步，速度更快，效果几乎一样。

## 残差连接：给信息和梯度一条直达通道

去掉残差，loss 会涨 0.33。整个 block 写成式子是这样：

<Math block>{`x_{l+1} = x_l + \\text{Sublayer}(\\text{LayerNorm}(x_l))`}</Math>

这个加号不止是数值上的“加回去”，它从两个方向起作用。

向前看，每一层都是在原始信息基础上做增量修改。哪怕这一层 attention 什么也没学到，输出接近 0，原信号仍可保留。堆叠 100 层时，第 1 层的 token embedding 仍可能直接出现在第 100 层输出中，模型无需每次从头重建输入。

向后看，反向传播的链式法则需要乘穿每一层的雅可比矩阵。如果中间某一层激活值很小，梯度就会被压扁成接近 0。残差相当于在主干上挂了一条恒等映射，梯度可以沿着这条 `x` 的路径不经过非线性变换直接传回去。深度模型能堆几十上百层，靠的就是这条直达通道。

## FFN：每个位置自己再消化一次

去掉 FFN 影响最小（loss +0.21），但去掉之后模型的表达能力会塌缩——它只剩下加权混合，没有非线性的“处理”能力。

FFN 结构为两层全连接，中间维度扩大 4 倍，再压缩回原维度。

<FFNExpansion />

为什么要先扩到 512 再缩回 128？高维度有更多“工作空间”，可以暂存复杂的中间表示。如果一直在 128 维里打转，能编码的状态有限。Transformer Feed-Forward Layers Are Key-Value Memories 那篇论文用电路视角去看，发现 FFN 第一层的每个神经元都对应一个特定的输入模式（key），第二层把对应的内容（value）写回。换句话说，模型大部分“事实知识”就存在 FFN 的权重里。Attention 决定去哪个书架取书，FFN 是那个书架。

代码就是 PyTorch 一个 `nn.Sequential`：

```python
self.net = nn.Sequential(
    nn.Linear(n_embd, 4 * n_embd),  # 升维
    nn.GELU(),                       # 非线性
    nn.Linear(4 * n_embd, n_embd),  # 降回
)
```

GELU 比 ReLU 平滑，负值不会直接截断为 0。后来 LLaMA 把这套换成了 SwiGLU——多一个 gate，参数量略增，效果更好，现在新的开源模型基本都用它。

## 位置编码：告诉模型谁前谁后

self-attention 本身完全不关心顺序。`Q · K` 是点积，对调两个 token 不会改变结果。这对语言是灾难——“cat sat on mat” 和 “mat sat on cat” 含义完全相反。

最简单的办法是给每个位置一个可学习的向量，加到 token embedding 上：

```python
tok_emb = self.token_embedding(idx)
pos_emb = self.position_embedding(pos)
x = tok_emb + pos_emb
```

模型于是同时拿到了两件事：这是什么 token、它在第几个位置。位置不同，下游 attention 看到的 Q/K 就不一样，输出也就不一样。

这种学到的绝对位置编码是 GPT-2 的做法，缺点是位置数量训练时就锁死了——ctx=64 的模型遇到第 65 个 token 时，没有可用的位置编码。后面那篇关于 RoPE 的文章会讲怎么把位置信息写入 Q/K 内部，让模型可以外推到训练时没见过的长度。

## 完整 GPT-2 风格模型结构

把所有零件装回去，一个完整的 decoder-only LLM 的骨架就是：

```python
x = token_embedding(idx) + position_embedding(pos)  # 进入主干
for block in self.blocks:                            # N 个 Transformer block
    x = block(x)
x = layer_norm_final(x)                              # 收尾再 norm 一次
logits = lm_head(x)                                  # 投影到 vocab，预测下一个 token
```

模型参数都长在哪？把 MiniGPT 的 42 万参数按模块拆开看：

<ParamBreakdownPie />

切换至 GPT-2 Small 可观察到：参数量相差三百倍，各模块占比几乎不变。FFN 约占一半，attention 约占四分之一，embedding 合计占大头。GPT-4 预计沿用相同比例继续扩展。模型扩容本质上是各模块按固定比例同步放大，整体结构不变。

## 代码

```text
minimal-llm/src/
  model.py      # MiniGPT 的完整实现：Block / MultiHeadAttention / FFN
  ablation.py   # 执行消融实验：去掉一个组件，观察 loss 变化
```

跑一次消融大约两分钟，会输出和上面那张柱状图一样的对比表：

```bash
python ablation.py
```

## 和工业界的距离

| 环节 | 这个项目 | 大模型 |
|------|---------|-----------|
| 层数 | 2 | 100+ |
| 维度 | 128 | 8000+ |
| 参数量 | 42 万 | 万亿 |
| Norm | Pre-LayerNorm | RMSNorm |
| FFN | GELU | SwiGLU |
| 位置编码 | 学到的绝对位置 | RoPE |
| Attention | 标准多头 | Grouped-Query Attention |

骨架完全一致，工业界的工作集中在替换零件：RMSNorm 比 LayerNorm 算得快、SwiGLU 比 GELU 表达力强一点、RoPE 解决长度外推、GQA 省 KV cache 显存。每一个改动都是单点优化，不动整体结构。

后面几篇会一个一个把这些升级的零件讲清楚，从位置编码 RoPE 开始。

## 动手实践

[在 Google Colab 上打开](https://colab.research.google.com/) （TODO：补上 ablation notebook 链接）

打开后选择 GPU runtime，整段消融实验约两分钟完成，可直接查看各变体的 loss 曲线。

## 参考论文

| 论文 | 说明 |
|------|------|
| [Attention Is All You Need · Vaswani, NeurIPS 2017](https://arxiv.org/abs/1706.03762) | 原始 Transformer 架构：残差 + LayerNorm + MHA + FFN |
| [On Layer Normalization in the Transformer Architecture · Xiong, ICML 2020](https://arxiv.org/abs/2002.04745) | Pre-Norm 比 Post-Norm 训练更稳定的理论分析 |
| [Transformer Feed-Forward Layers Are Key-Value Memories · Geva, EMNLP 2021](https://arxiv.org/abs/2012.14913) | 把 FFN 解释成键值存储，定位事实知识在哪 |
| [LLaMA: Open and Efficient Foundation Language Models · Touvron, Meta 2023](https://arxiv.org/abs/2302.13971) | 现代 Transformer 配方：RMSNorm + SwiGLU + RoPE |
