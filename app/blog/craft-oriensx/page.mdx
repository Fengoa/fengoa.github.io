export const metadata = {
  title: "搭建这个网站",
}

# 搭建这个网站

动手之前先看了一圈别人的网站。

1. [Rauno](https://rauno.me) 的交互和动效很惊艳，我想要的是安静但带点小活气的感觉，参考它的运动设计。

2. [Paco](https://paco.me) 的留白和结构舒服，配图也讲究，看看怎么处理空间。
3. [Lee Robinson](https://leerob.com) 结构清晰、跳转干脆，导航做得很克制。

4. [Vercel](https://vercel.com) 的网格让页面有秩序感，是我想要的底子。

5. [wxad.design/abc](https://wxad.design/abc) 的交互很有分寸，让我受益匪浅。


## 网格系统

我做的第一件事是画格子。

我想要的更接近建筑图纸上的参考线。我们写字的时候不会去看格线，但格线让字不会歪到一边去，网页上也是同理。

搭了一套 `Grid` 复合组件，技术实现的基本思路来自 Vercel 设计师的 [What will you ship?](https://rauno.me/craft/vercel#grid-system)。

画参考线这件事，乍看简单，其实有个根本矛盾。

最常见的做法是：给每个格子画右边框和下边框，再给容器补上上边框和左边框，拼起来就是完整网格。但这种方式要求**每个格子都必须有对应的 DOM 节点**——你得老老实实写满 `rows × columns` 个空 div。

而我们想要的是：只声明行列数，只放需要放内容的格子，参考线自动出现。稀疏布局、跨行跨列的单元格，都要能正常工作。

关键在 `display: contents`。这个属性**让元素的子节点「穿透」到父级**，在布局上仿佛父元素不存在。

对网格来说：参考线层用一个 `display: contents` 的容器包住，里面渲染 `rows × columns` 个绝对定位的 div，每个用 `grid-column-start` / `grid-row-start` 占住对应格子，画右边框和下边框。

容器本身不参与布局，这些 div 在视觉上铺满网格，又不影响真正的子元素排布。而且因为参考线是独立的一层，`Grid.Cell` 用 `row="auto"`、`column="auto"` 时也不会把网格搞乱——没有 `display: contents` 的话，这种写法会直接崩掉。

```tsx
<div className="grid relative">
  {/* 参考线层：contents 让子元素参与网格布局，父容器不占位 */}
  <div className="contents">
    {Array.from({ length: rows * columns }, (_, i) => {
      const x = (i % columns) + 1;
      const y = Math.floor(i / columns) + 1;
      return (
      <div
        key={i}
        className="absolute inset-0 pointer-events-none"
        style={{
          gridColumnStart: x,
          gridRowStart: y,
          borderRight: "var(--gw, 1px) solid var(--border)",
          borderBottom: "var(--gw, 1px) solid var(--border)",
        }}
      />
      );
    })}
  </div>;
  {children}
</div>
```

**网格**

`Grid.System` 是页面根容器，用 CSS 变量 `--guide-width` 控制线宽。

相邻 Grid 接壤时边线会叠成双倍粗，用负 margin 抵消：`[&>.grid+.grid]:mt-[calc(var(--guide-width)*-1)]`。

<DemoWithCode
  code={`<Grid.System className="items-center">
    <Grid rows={1} columns={2}>
      <Grid.Cell row={1} column={1} />
      <Grid.Cell row={1} column={2} />
    </Grid>
    <Grid rows={1} columns={4}>
      <Grid.Cell row={1} column={1} />
      <Grid.Cell row={1} column={2} />
      <Grid.Cell row={1} column={3} />
      <Grid.Cell row={1} column={4} />
    </Grid>
</Grid.System>`}
>
  <Grid.System className="items-center">
    <Grid rows={1} columns={2} className="w-25">
      <Grid.Cell row={1} column={1} className="aspect-square" />
      <Grid.Cell row={1} column={2} className="aspect-square" />
    </Grid>
    <Grid rows={1} columns={4} className="w-50">
      <Grid.Cell row={1} column={1} className="aspect-square" />
      <Grid.Cell row={1} column={2} className="aspect-square" />
      <Grid.Cell row={1} column={3} className="aspect-square" />
      <Grid.Cell row={1} column={4} className="aspect-square" />
    </Grid>
  </Grid.System>
</DemoWithCode>

**合并单元格**

合并单元格时，跨多格的区域内部不该出现参考线。

`getHiddenBorders` 遍历合并区域，判断当前格是否在区域内部，是的话把对应右边框或下边框设为 `none`。做法朴素，但能正确处理 Hero 那种「第二行中间 10 格合并放标题」的情况。

<DemoWithCode
  code={`<Grid
    rows={4}
    columns={4}
    mergedAreas={[{ row: 2, column: 2, rowSpan: 2, colSpan: 2 }]}
  >
  {[
    <Grid.Cell key="2-1" row={2} column={1} />,
    <Grid.Cell key="2-4" row={2} column={4} />,
    <Grid.Cell row={2} column={2} rowSpan={2} colSpan={2}>
    合并
    </Grid.Cell>
    <Grid.Cell key="3-1" row={3} column={1} />,
    <Grid.Cell key="3-4" row={3} column={4} />,
  ]}
 
</Grid>`}
>
  <Grid
    rows={4}
    columns={4}
    mergedAreas={[{ row: 2, column: 2, rowSpan: 2, colSpan: 2 }]}
    className="w-50"
  >
    {[
      ...[1, 2, 3, 4].map((c) => (
        <Grid.Cell
          key={`1-${c}`}
          row={1}
          column={c}
          className="aspect-square"
        />
      )),
      <Grid.Cell key="2-1" row={2} column={1} className="aspect-square" />,
      <Grid.Cell key="2-4" row={2} column={4} className="aspect-square" />,
      <Grid.Cell key="3-1" row={3} column={1} className="aspect-square" />,
      <Grid.Cell key="3-4" row={3} column={4} className="aspect-square" />,
      ...[1, 2, 3, 4].map((c) => (
        <Grid.Cell
          key={`4-${c}`}
          row={4}
          column={c}
          className="aspect-square"
        />
      )),
    ]}
    <Grid.Cell
      row={2}
      column={2}
      rowSpan={2}
      colSpan={2}
      className="aspect-square flex items-center justify-center bg-muted/50 text-xs"
    >
      合并
    </Grid.Cell>
  </Grid>
</DemoWithCode>

**十字标注**

`Grid.Cross` 在指定行列的交叉点画 20×20 的 SVG 十字，`strokeWidth` 0.6，给网格加点建筑图纸的标注感。

<DemoWithCode
  code={`<Grid rows={4} columns={4}>
    <Grid.Cross column={1} row={1} />
    <Grid.Cross column={2} row={5} />
</Grid>`}
>
  <Grid rows={2} columns={4} className="w-50">
    {Array.from({ length: 8 }, (_, i) => (
      <Grid.Cell
        key={i}
        row={Math.floor(i / 4) + 1}
        column={(i % 2) + 1}
        className="aspect-square"
      />
    ))}
    <Grid.Cross column={1} row={1} />
    <Grid.Cross column={5} row={3} />
  </Grid>
</DemoWithCode>

**响应式**

响应式比较麻烦。桌面 12 列、移动端 1～2 列，列数一变参考线就得重算。桌面和移动各生成一套，用 `max-md:hidden` / `md:hidden` 切换。

`grid-template-columns` 是运行时动态的，Tailwind 的响应式类覆盖不了，只好用 `<style>` 注入媒体查询。不是很优雅，但管用，先用着。

## 边框光束：80 秒的呼吸

网格让页面有了秩序，但秩序太强会显得冷。我想加一点不那么安分的东西，让页面有一点点活气。安静的房间里有秒针在走，你不会嫌它吵，但它让安静变得"可感知"。这是边框光束想做的事。

<div className="my-8 overflow-hidden rounded relative h-40 bg-card flex items-center justify-center">
  <BorderBeam duration={80} size={50} />
  <div className="text-muted-foreground/20 text-4xl">致 敬 浪 漫</div>
</div>

从 [Magic UI](https://magicui.design/docs/components/border-beam) 找来了 `BorderBeam`，一小截带渐变的光束，沿着视口边缘走，80 秒一圈。这个速度你不会主动注意到，但去掉就会觉得少了点什么。

## 导航悬停：别急

我做导航的时候一直在想一个问题：悬停反馈要有多大，我觉得"立刻"有点急。

我想要一种感觉：你把鼠标放上去，什么都不发生，或者微弱的感觉，好像这个按钮不太在意你来没来。等一秒，图标慢慢动了一下；再等半秒，文字上有一道光扫过去，像是在提醒你，怎么还不点击，来了就点击一下吧。然后安静两秒，再循环。

写了个 `useHoverSequence` 来编排这件事。核心就是嵌套的 `setTimeout`，每一步结束时通过 `hoveredRef` 检查鼠标是否还在，不在就停下来：

```ts
const playSequence = useCallback(() => {
  iconRef.current?.startAnimation();
  addTimer(() => {
    if (!hoveredRef.current) return;
    setIsShimmering(true);
    addTimer(() => {
      setIsShimmering(false);
      addTimer(() => {
        if (!hoveredRef.current) return;
        playSequence();
      }, loopDelay);
    }, shimmerDuration);
  }, shimmerDelay);
}, [addTimer, shimmerDelay, shimmerDuration, loopDelay]);
```

鼠标离开的瞬间，`handleLeave` 会一次性清掉所有定时器、停掉图标动画、关掉文字闪光。不等当前动画播完，直接切断。这样即使你快速在几个导航之间划过，也不会残留上一个的动画状态。

文字闪光那个效果叫 `TextShimmer`，可以从 [Motion Primitives](https://motion-primitives.com/docs/text-shimmer) 找到。

原理是用了双层 `background-image` 配合 `background-clip: text`：底层一个纯色 `linear-gradient` 做基色，上层一条窄窄的高亮渐变带，通过 `backgroundPosition` 从右扫到左。

<DemoWithCode
  code={`const seq = useHoverSequence();
// → { iconRef, isShimmering, handleEnter, handleLeave }

// 绑定到按钮
onMouseEnter={seq.handleEnter}
onMouseLeave={seq.handleLeave}

// 图标通过 ref 受控
<BrainIcon ref={seq.iconRef} isAnimated={false} />

// 根据 isShimmering 切换文字渲染
{seq.isShimmering
  ? <TextShimmer>博客</TextShimmer>
  : <span>博客</span>}`}
  demoClassName="min-h-[80px]"
>
  <NavHoverDemo />
</DemoWithCode>

再之后我把这个效果用在了链接上。

## 最后，Be quiet

大部分时间花在了用户根本注意不到的地方：80 秒一圈的光束、悬停 1 秒后才出现的动画、对齐到像素的网格、导航上那道几乎看不见的模糊边。单拿出来都不值一提，加在一起会让人觉得"舒服"，但说不上来哪里舒服。

页脚的 "Be quiet." 差不多就是我现在想说的。
